THIẾT KẾ ĐƠN GIẢN CLASS DIAGRAM CHO CHỨC NĂNG VIEW BOOKING REQUEST (RECEPTIONIST)
DÀNH CHO VISUAL PARADIGM

--------------------------------------------------------------------------------
1. DANH SÁCH CÁC LỚP VÀ CẤU TRÚC CHI TIẾT
--------------------------------------------------------------------------------

A. Lớp Controller:
- Class Name: ReceptionistBookingDetailController
- Stereotype: <<Servlet>>
- Parent Class (Kế thừa): HttpServlet (jakarta.servlet.http.HttpServlet)
- Attributes:
  * - LOGGER : Logger (java.util.logging.Logger)
- Operations:
  * # doGet(request : HttpServletRequest, response : HttpServletResponse) : void

B. Lớp Data Access Object (DAO):
- Class Name: BookingDAO
- Attributes:
  * - LOGGER : Logger
  * - STATUS_WHITELIST : Set<String>
  * - FILTER_STATUS_WHITELIST : Set<String>
- Operations:
  * + getBookingById(bookingId : int) : Booking
  * + getCustomerDetailsByAccountId(accountId : int) : CustomerDetails
  * + getAssignedRoomsForBooking(bookingId : int) : List<Room>
  * - ensureBookingRoomTableExists() : void

C. Lớp Thực thể (Entities):
- Class Name: Booking
- Attributes:
  * - bookingId : int
  * - accountId : Integer
  * - customerName : String
  * - roomTypeId : Integer
  * - roomTypeName : String
  * - roomQuantity : int
  * - checkInDate : Date
  * - checkOutDate : Date
  * - totalAmount : double
  * - status : String
  * - note : String
  * - createdAt : Date
  * - assignedRoomsStr : String
- Operations:
  * + getNights() : long
  * + isValid() : boolean

- Class Name: Room
- Attributes:
  * - roomId : int
  * - roomNumber : String
  * - status : String
  * - floor : String
  * - typeName : String

- Class Name: CustomerDetails
- Attributes:
  * - fullName : String
  * - email : String
  * - phone : String

D. Lớp Kết nối Cơ sở Dữ liệu:
- Class Name: DBContext
- Operations:
  * + getConnection() : Connection

--------------------------------------------------------------------------------
2. MỐI QUAN HỆ GIỮA CÁC LỚP (RELATIONSHIPS)
--------------------------------------------------------------------------------

- Kế thừa (Generalization - Nét liền, mũi tên tam giác rỗng):
  * ReceptionistBookingDetailController ---> HttpServlet

- Phụ thuộc (Dependency - Nét đứt, mũi tên chữ V):
  * ReceptionistBookingDetailController ---> BookingDAO
  * ReceptionistBookingDetailController ---> Booking
  * ReceptionistBookingDetailController ---> Room
  * ReceptionistBookingDetailController ---> CustomerDetails

  * BookingDAO ---> DBContext
  * BookingDAO ---> Booking
  * BookingDAO ---> Room
  * BookingDAO ---> CustomerDetails

--------------------------------------------------------------------------------
3. MÃ NGUỒN SƠ ĐỒ MERMAID (Có thể copy vào các tool hỗ trợ xem sơ đồ)
--------------------------------------------------------------------------------

classDiagram
    class HttpServlet {
        <<Servlet>>
        #doGet(request: HttpServletRequest, response: HttpServletResponse): void
        #doPost(request: HttpServletRequest, response: HttpServletResponse): void
    }

    class ReceptionistBookingDetailController {
        -LOGGER: Logger
        #doGet(request: HttpServletRequest, response: HttpServletResponse): void
    }

    class BookingDAO {
        -LOGGER: Logger
        -STATUS_WHITELIST: Set~String~
        -FILTER_STATUS_WHITELIST: Set~String~
        -ensureBookingRoomTableExists(): void
        +getBookingById(bookingId: int): Booking
        +getCustomerDetailsByAccountId(accountId: int): CustomerDetails
        +getAssignedRoomsForBooking(bookingId: int): List~Room~
    }

    class DBContext {
        +getConnection(): Connection
    }

    class Booking {
        -bookingId: int
        -accountId: Integer
        -customerName: String
        -roomTypeId: Integer
        -roomTypeName: String
        -roomQuantity: int
        -checkInDate: Date
        -checkOutDate: Date
        -totalAmount: double
        -status: String
        -note: String
        -createdAt: Date
        -assignedRoomsStr: String
        +getNights(): long
        +isValid(): boolean
    }

    class Room {
        -roomId: int
        -roomNumber: String
        -status: String
        -floor: String
        -typeName: String
    }

    class CustomerDetails {
        -fullName: String
        -email: String
        -phone: String
    }

    HttpServlet <|-- ReceptionistBookingDetailController : Inheritance
    ReceptionistBookingDetailController ..> BookingDAO : Dependency
    ReceptionistBookingDetailController ..> Booking : Dependency
    ReceptionistBookingDetailController ..> Room : Dependency
    ReceptionistBookingDetailController ..> CustomerDetails : Dependency
    
    BookingDAO ..> DBContext : Dependency
    BookingDAO ..> Booking : Dependency
    BookingDAO ..> Room : Dependency
    BookingDAO ..> CustomerDetails : Dependency
